import { IApplicationCommandData } from '@tryforge/forgescript';

const ApplicationCommand: IApplicationCommandData = {
  data: {
    type: 1,
    name: 'status',
    description: 'Check the current anti-raid status and settings.'
  },
  code: ``,
};

export default ApplicationCommand;